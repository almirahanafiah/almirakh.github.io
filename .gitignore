# Internal / behind-the-scenes (kept local, never published)
CLAUDE.md
docs/

# Case 03 source exports (kept local), except the public cover used by the
# index card and the bootcamp preview, which must be published to deploy.
assets/case-03/*
!assets/case-03/gaia-cover.mp4
!assets/case-03/gaia-cover.jpg

# macOS noise
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/
.claude/

# Logs and dependency dirs (no build step yet, but here just in case)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (in case you ever add a deploy script with secrets)
.env
.env.local
.env.*.local

# Output files from local tools
*.log
.cache/
.tmp/
.parcel-cache/
dist/
build/
